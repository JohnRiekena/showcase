<div class="card mb-3">
  <div class="card-body" data-action="click->example#load_home_world" data-homeworld="<%= person.homeworld_id %>">
    Name: <%= person.name %>
    Weight: <%= person.mass %>
    Gender: <%= person.gender %>
  </div>
  <%# I just didn't want this link on every card, so limiting it for Luke (and his family) %>
  <% if person.name.include? 'Skywalker' %>
    <div class="mb-1 text-center">
      <%# this is just to show that I can also update world by targeting turboframe directly from a link %>
      <%= link_to 'Update World', example_homeworld_path(person.homeworld_id), data: { turbo_frame: :home_world } %>
    </div>
  <% end %>
</div>